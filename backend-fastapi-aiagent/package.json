{
  "name": "backend-fastapi-aiagent",
  "version": "1.0.0",
  "scripts": {
    "_本地可以切換以下": "--reload不用重啟後端,上線指定port比較好(通常--port=10000)",
    "_start": "uvicorn app.main:app --reload",
    "start": "uvicorn app.main:app --port=8000"
  },
  "devDependencies": {}
}